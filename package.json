{
	"name": "arnstien-protocol",
	"version": "1.3.0",
	"description": "Encrypts pasted images with per-image keys, stores them outside the vault, and seeds them over BitTorrent DHT.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"keywords": [],
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.15.17",
		"@types/webtorrent": "^0.110.2",
		"esbuild": "0.25.5",
		"obsidian": "latest",
		"typescript": "^5.8.3"
	},
	"dependencies": {
		"webtorrent": "^1.9.7"
	}
}
